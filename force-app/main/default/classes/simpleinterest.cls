public class simpleinterest {

    public static void calc(){
        decimal prin=50000, rate=50, t=3;
        decimal si= (prin*rate*t)/100;
        system.debug('Simple Interest = ' +si);
        
    }
}